import { RandomFilledBoxCountPipe } from './random-filled-box-count.pipe';

describe('RandomFilledBoxCountPipe', () => {
  it('create an instance', () => {
    const pipe = new RandomFilledBoxCountPipe();
    expect(pipe).toBeTruthy();
  });
});
